import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Sebastian Liu — Software Engineer",
  description:
    "Software engineer building full-stack products end to end. Portfolio featuring a production Spotify web app and a computer-vision system built from scratch.",
  openGraph: {
    title: "Sebastian Liu — Software Engineer",
    description:
      "Full-stack software engineer — typed React frontends, Python backends, shipped and deployed.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
